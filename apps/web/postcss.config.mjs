// [WP12 · S12-21] Named so the export is greppable and the config is
// importable from a test.
const postcssConfig = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default postcssConfig;
