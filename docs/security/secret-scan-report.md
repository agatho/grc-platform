# Secret-Scan Report

_Generated: 2026-09-01T15:34:45.361Z_

Files scanned: 3901. Findings: 0.

✅ **No potential secrets found.**

This scan uses conservative regex patterns (see scripts/audit-secrets.mjs). It is NOT a guarantee of secret-free code -- pair with gitleaks (CI) and GitHub Secret-Scanning.

## Bekannte, bewertete Testfixtures (49)

Diese Treffer sind fachlich notwendig und geprüft. Sie stehen bewusst NICHT in der Fundliste oben: bis zum Audit 2026-08-31 meldete dieser Report dauerhaft dieselben sechs Treffer, und genau deshalb wurde er nicht mehr gelesen (#S08-14). Ein NEUER Treffer erscheint oben und fällt damit auf.

| File                                                             | Line | Pattern                                   | Begründung                                                                                                                                                                                                                                                                        |
| ---------------------------------------------------------------- | ---- | ----------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `.env.example`                                                   | 18   | DB/Broker connection string with password | Template mit Platzhaltern; scripts/check-env-example.mjs erzwingt, dass kein Platzhalter wie ein Wert aussieht (#S08-18).                                                                                                                                                         |
| `.env.example`                                                   | 28   | DB/Broker connection string with password | Template mit Platzhaltern; scripts/check-env-example.mjs erzwingt, dass kein Platzhalter wie ein Wert aussieht (#S08-18).                                                                                                                                                         |
| `.github/workflows/ci.yml`                                       | 202  | DB/Broker connection string with password | CI-Testpasswoerter gegen den Service-Container des Runners.                                                                                                                                                                                                                       |
| `.github/workflows/ci.yml`                                       | 242  | DB/Broker connection string with password | CI-Testpasswoerter gegen den Service-Container des Runners.                                                                                                                                                                                                                       |
| `.github/workflows/ci.yml`                                       | 273  | DB/Broker connection string with password | CI-Testpasswoerter gegen den Service-Container des Runners.                                                                                                                                                                                                                       |
| `.github/workflows/ci.yml`                                       | 299  | DB/Broker connection string with password | CI-Testpasswoerter gegen den Service-Container des Runners.                                                                                                                                                                                                                       |
| `.github/workflows/ci.yml`                                       | 305  | DB/Broker connection string with password | CI-Testpasswoerter gegen den Service-Container des Runners.                                                                                                                                                                                                                       |
| `.github/workflows/ci.yml`                                       | 310  | DB/Broker connection string with password | CI-Testpasswoerter gegen den Service-Container des Runners.                                                                                                                                                                                                                       |
| `.github/workflows/ci.yml`                                       | 330  | DB/Broker connection string with password | CI-Testpasswoerter gegen den Service-Container des Runners.                                                                                                                                                                                                                       |
| `.github/workflows/ci.yml`                                       | 331  | DB/Broker connection string with password | CI-Testpasswoerter gegen den Service-Container des Runners.                                                                                                                                                                                                                       |
| `.github/workflows/ci.yml`                                       | 346  | DB/Broker connection string with password | CI-Testpasswoerter gegen den Service-Container des Runners.                                                                                                                                                                                                                       |
| `.github/workflows/ci.yml`                                       | 347  | DB/Broker connection string with password | CI-Testpasswoerter gegen den Service-Container des Runners.                                                                                                                                                                                                                       |
| `.github/workflows/ci.yml`                                       | 377  | DB/Broker connection string with password | CI-Testpasswoerter gegen den Service-Container des Runners.                                                                                                                                                                                                                       |
| `.github/workflows/ci.yml`                                       | 419  | DB/Broker connection string with password | CI-Testpasswoerter gegen den Service-Container des Runners.                                                                                                                                                                                                                       |
| `.github/workflows/ci.yml`                                       | 581  | DB/Broker connection string with password | CI-Testpasswoerter gegen den Service-Container des Runners.                                                                                                                                                                                                                       |
| `.github/workflows/ci.yml`                                       | 621  | DB/Broker connection string with password | CI-Testpasswoerter gegen den Service-Container des Runners.                                                                                                                                                                                                                       |
| `.github/workflows/ci.yml`                                       | 651  | DB/Broker connection string with password | CI-Testpasswoerter gegen den Service-Container des Runners.                                                                                                                                                                                                                       |
| `.github/workflows/migration-policy.yml`                         | 156  | DB/Broker connection string with password | Bindet an `localhost` — localhost bzw. ein Compose-Servicename. Kein Produktivbezug; die produktiven Passwörter erzeugen deploy/setup-hetzner.sh und deploy/create-tenant.sh per `openssl rand` (#S08-18).                                                                        |
| `.github/workflows/migration-policy.yml`                         | 162  | DB/Broker connection string with password | Bindet an `localhost` — localhost bzw. ein Compose-Servicename. Kein Produktivbezug; die produktiven Passwörter erzeugen deploy/setup-hetzner.sh und deploy/create-tenant.sh per `openssl rand` (#S08-18).                                                                        |
| `.github/workflows/migration-policy.yml`                         | 173  | DB/Broker connection string with password | Bindet an `localhost` — localhost bzw. ein Compose-Servicename. Kein Produktivbezug; die produktiven Passwörter erzeugen deploy/setup-hetzner.sh und deploy/create-tenant.sh per `openssl rand` (#S08-18).                                                                        |
| `.github/workflows/schema-drift.yml`                             | 104  | DB/Broker connection string with password | Bindet an `localhost` — localhost bzw. ein Compose-Servicename. Kein Produktivbezug; die produktiven Passwörter erzeugen deploy/setup-hetzner.sh und deploy/create-tenant.sh per `openssl rand` (#S08-18).                                                                        |
| `.github/workflows/schema-drift.yml`                             | 109  | DB/Broker connection string with password | Bindet an `localhost` — localhost bzw. ein Compose-Servicename. Kein Produktivbezug; die produktiven Passwörter erzeugen deploy/setup-hetzner.sh und deploy/create-tenant.sh per `openssl rand` (#S08-18).                                                                        |
| `CLAUDE.md`                                                      | 454  | DB/Broker connection string with password | Bindet an `localhost` — localhost bzw. ein Compose-Servicename. Kein Produktivbezug; die produktiven Passwörter erzeugen deploy/setup-hetzner.sh und deploy/create-tenant.sh per `openssl rand` (#S08-18).                                                                        |
| `SETUP.md`                                                       | 20   | DB/Broker connection string with password | Bindet an `localhost` — localhost bzw. ein Compose-Servicename. Kein Produktivbezug; die produktiven Passwörter erzeugen deploy/setup-hetzner.sh und deploy/create-tenant.sh per `openssl rand` (#S08-18).                                                                        |
| `SETUP.md`                                                       | 34   | DB/Broker connection string with password | Bindet an `localhost` — localhost bzw. ein Compose-Servicename. Kein Produktivbezug; die produktiven Passwörter erzeugen deploy/setup-hetzner.sh und deploy/create-tenant.sh per `openssl rand` (#S08-18).                                                                        |
| `apps/web/e2e/document-signature.spec.ts`                        | 31   | Generic password assignment               | Playwright-Zugangsdaten fuer das Demo-Seed-Konto der Wegwerf-E2E-DB. Der Demo-Seed verweigert sich in NODE_ENV=production (#SEC-F04).                                                                                                                                             |
| `apps/web/src/__tests__/rls-route-chain/risks-route-rls.test.ts` | 109  | DB/Broker connection string with password | Bindet an `localhost` — localhost bzw. ein Compose-Servicename. Kein Produktivbezug; die produktiven Passwörter erzeugen deploy/setup-hetzner.sh und deploy/create-tenant.sh per `openssl rand` (#S08-18).                                                                        |
| `apps/web/src/app/(dashboard)/access-log/page.tsx`               | 71   | Generic password assignment               | Kein Secret: Label-Mapping der Anmeldemethode fuer die Oberflaeche (Schluessel `password` auf seinen Anzeigetext).                                                                                                                                                                |
| `deploy/.env.production.example`                                 | 14   | DB/Broker connection string with password | Bindet an `postgres` — localhost bzw. ein Compose-Servicename. Kein Produktivbezug; die produktiven Passwörter erzeugen deploy/setup-hetzner.sh und deploy/create-tenant.sh per `openssl rand` (#S08-18).                                                                         |
| `docs/DEVELOPER_GUIDE.md`                                        | 362  | DB/Broker connection string with password | Bindet an `localhost` — localhost bzw. ein Compose-Servicename. Kein Produktivbezug; die produktiven Passwörter erzeugen deploy/setup-hetzner.sh und deploy/create-tenant.sh per `openssl rand` (#S08-18).                                                                        |
| `docs/DEV_ENVIRONMENT_GUIDE.md`                                  | 153  | DB/Broker connection string with password | Bindet an `localhost` — localhost bzw. ein Compose-Servicename. Kein Produktivbezug; die produktiven Passwörter erzeugen deploy/setup-hetzner.sh und deploy/create-tenant.sh per `openssl rand` (#S08-18).                                                                        |
| `docs/DEV_ENVIRONMENT_GUIDE.md`                                  | 160  | DB/Broker connection string with password | Bindet an `localhost` — localhost bzw. ein Compose-Servicename. Kein Produktivbezug; die produktiven Passwörter erzeugen deploy/setup-hetzner.sh und deploy/create-tenant.sh per `openssl rand` (#S08-18).                                                                        |
| `packages/auth/tests/fixtures/idp-test-key.pem`                  | 1    | Private Key Header                        | Eigens fuer die SAML-Testsuite erzeugtes IdP-Schluesselpaar (WP3). Es gehoert zu keiner realen Identitaet und wird von keinem Deployment gelesen — die produktive IdP-Konfiguration kommt aus sso_config. Bewusst im Repo, damit die Signaturpruefungstests deterministisch sind. |
| `packages/auth/tests/fixtures/idp-test-key.pem`                  | 1    | Certificate/Key file content              | Eigens fuer die SAML-Testsuite erzeugtes IdP-Schluesselpaar (WP3). Es gehoert zu keiner realen Identitaet und wird von keinem Deployment gelesen — die produktive IdP-Konfiguration kommt aus sso_config. Bewusst im Repo, damit die Signaturpruefungstests deterministisch sind. |
| `packages/db/sql/fix_umlauts_v3.sql`                             | 3    | Generic password assignment               | Dev-Aufrufbeispiel im Kopfkommentar (localhost, grc_dev_password).                                                                                                                                                                                                                |
| `packages/db/tests/helpers.ts`                                   | 7    | DB/Broker connection string with password | Dev-Datenbank-URL gegen localhost.                                                                                                                                                                                                                                                |
| `packages/db/tests/integration/audit-chain-per-tenant.test.ts`   | 8    | DB/Broker connection string with password | Bindet an `localhost` — localhost bzw. ein Compose-Servicename. Kein Produktivbezug; die produktiven Passwörter erzeugen deploy/setup-hetzner.sh und deploy/create-tenant.sh per `openssl rand` (#S08-18).                                                                        |
| `packages/db/tests/integration/audit-chain-per-tenant.test.ts`   | 353  | DB/Broker connection string with password | Bindet an `localhost` — localhost bzw. ein Compose-Servicename. Kein Produktivbezug; die produktiven Passwörter erzeugen deploy/setup-hetzner.sh und deploy/create-tenant.sh per `openssl rand` (#S08-18).                                                                        |
| `packages/db/tests/integration/audit-integrity-live.test.ts`     | 6    | DB/Broker connection string with password | Bindet an `localhost` — localhost bzw. ein Compose-Servicename. Kein Produktivbezug; die produktiven Passwörter erzeugen deploy/setup-hetzner.sh und deploy/create-tenant.sh per `openssl rand` (#S08-18).                                                                        |
| `packages/db/tests/integration/audit-tamper-evidence.test.ts`    | 27   | DB/Broker connection string with password | Bindet an `localhost` — localhost bzw. ein Compose-Servicename. Kein Produktivbezug; die produktiven Passwörter erzeugen deploy/setup-hetzner.sh und deploy/create-tenant.sh per `openssl rand` (#S08-18).                                                                        |
| `packages/db/tests/integration/audit-tamper-evidence.test.ts`    | 34   | DB/Broker connection string with password | Bindet an `localhost` — localhost bzw. ein Compose-Servicename. Kein Produktivbezug; die produktiven Passwörter erzeugen deploy/setup-hetzner.sh und deploy/create-tenant.sh per `openssl rand` (#S08-18).                                                                        |
| `packages/db/tests/integration/gdpr-privacy.test.ts`             | 14   | DB/Broker connection string with password | Bindet an `localhost` — localhost bzw. ein Compose-Servicename. Kein Produktivbezug; die produktiven Passwörter erzeugen deploy/setup-hetzner.sh und deploy/create-tenant.sh per `openssl rand` (#S08-18).                                                                        |
| `packages/db/tests/integration/gdpr-privacy.test.ts`             | 21   | DB/Broker connection string with password | Bindet an `localhost` — localhost bzw. ein Compose-Servicename. Kein Produktivbezug; die produktiven Passwörter erzeugen deploy/setup-hetzner.sh und deploy/create-tenant.sh per `openssl rand` (#S08-18).                                                                        |
| `packages/shared/src/url-safety.ts`                              | 298  | HTTP Basic-Auth URL                       | Bindet an `internal` — localhost bzw. ein Compose-Servicename. Kein Produktivbezug; die produktiven Passwörter erzeugen deploy/setup-hetzner.sh und deploy/create-tenant.sh per `openssl rand` (#S08-18).                                                                         |
| `packages/shared/tests/identity-schemas.test.ts`                 | 163  | Generic password assignment               | Zod-Schema-Test der Passwortregeln — der Wert IST der Testgegenstand.                                                                                                                                                                                                             |
| `packages/shared/tests/schemas.test.ts`                          | 457  | Generic password assignment               | Zod-Schema-Tests der Passwortregeln — die Werte SIND der Testgegenstand.                                                                                                                                                                                                          |
| `packages/shared/tests/schemas.test.ts`                          | 474  | Generic password assignment               | Zod-Schema-Tests der Passwortregeln — die Werte SIND der Testgegenstand.                                                                                                                                                                                                          |
| `packages/shared/tests/schemas.test.ts`                          | 505  | Generic password assignment               | Zod-Schema-Tests der Passwortregeln — die Werte SIND der Testgegenstand.                                                                                                                                                                                                          |
| `scripts/audit-rls-coverage.mjs`                                 | 54   | DB/Broker connection string with password | Bindet an `localhost` — localhost bzw. ein Compose-Servicename. Kein Produktivbezug; die produktiven Passwörter erzeugen deploy/setup-hetzner.sh und deploy/create-tenant.sh per `openssl rand` (#S08-18).                                                                        |

## Grenzen dieses Scans

Dieser Scan läuft ausschließlich über den ARBEITSBAUM (HEAD). Ein einmal committetes und später entferntes Secret ist hier unsichtbar. Den vollständigen Historien-Scan fährt wöchentlich der Job `full-history-scan` in `.github/workflows/secret-scanning.yml` (gitleaks über alle Refs, Ergebnis als Artefakt, 90 Tage) — siehe #S08-14 und #S08-15.
