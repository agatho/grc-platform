{
  // [ARCTOS-FULL-2026-08-31 / Welle 4b, Strang 6 · OP-065] Die Messung oben
  // stammt vom 2026-09-01 und war NICHT die Zahl dieses Pakets: sie zählte
  // beide Schalter zusammen und, weil beim Einschalten in einem Paket auch
  // die Quellen seiner Geschwisterpakete in das Programm geraten, dieselben
  // Fundstellen in `shared` und `db` in jedem Paket erneut mit. Neu gemessen
  // am 2026-09-03, nur `noUncheckedIndexedAccess`, je Paket die EIGENEN
  // Dateien:
  //
  //   Paket        eigene Fehler   davon src   davon tests
  //   shared            278            172         106
  //   db                452             57         395
  //   ai                 80              9          71
  //   auth               30             28           2
  //   automation         17             17           0
  //   events              8              0           8
  //   email               8              2           6
  //   reporting           4              4           0
  //   graph               3              3           0
  //   ui                  0              0           0
  //
  // Alle abgetragen; `noUncheckedIndexedAccess` steht deshalb wieder auf dem
  // ererbten `true` aus tsconfig.base.json.
  // Belege: docs/UMSETZUNG-WELLE-4B-6.md
  //
  // [N-2 · Welle 6a] `noUnusedLocals` steht jetzt ebenfalls auf dem ererbten
  // `true`. Gemessen am 2026-09-07: 1 eigene Fundstelle(n) in diesem Paket,
  // alle abgetragen — keine per `!`, keine per `as`, keine per
  // `// @ts-expect-error`. Belege und die dabei freigelegten Produktdefekte:
  // docs/UMSETZUNG-WELLE-6A.md

  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    "types": ["node"],
    "lib": ["ES2022"]
  },
  "include": ["src/**/*.ts"]
}
